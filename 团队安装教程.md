# 短剧分镜自动化 · 团队安装使用教程

> 照着从上往下做。全程在**浏览器**里,不用在电脑上装任何软件。
> 卡在哪一步,就把那一屏截图发给负责人。

---

## 开始前,先懂三件事(很重要)

**1. 这套东西全在浏览器里跑,什么电脑都能用。**
Windows 能用,苹果电脑能用,**苹果 M 芯片(M1/M2/M3/M4)也能用**,和是不是英特尔芯片没关系。因为真正干活的程序在云端(网上),你的电脑只负责开网页。**只要能上网、有浏览器(Chrome / Edge / Safari 都行)就行。**

**2. 我们要用两个网站,它们是"网站",不是要下载安装的软件。**
- `github.com` —— 存文件的地方(存剧本、存规范、存跑出来的表)。
- `claude.ai/code` —— 干活的地方(在这儿让 AI 跑分镜)。
两个都是打开浏览器直接访问,**不用装**。

**3. 你需要一个 Claude Max 账号。**
网页版 Claude Code 只有 **Pro / Max / Team** 付费账号能用,免费账号用不了。开工前先确认你的账号是这几档之一。

---

## 【第一部分】建一个 GitHub 仓库(每台机子做一次)

### 第 1 步 · 注册 / 登录 GitHub
浏览器打开 `github.com`,注册或登录一个账号。

### 第 2 步(苹果和 Windows 都要做)· 关掉浏览器"自动翻译"
浏览器的自动翻译会把网页文字(甚至你的文件夹名)翻译得乱七八糟,后面很容易看错、点错。**必须先关掉:**
- 地址栏右边有个翻译图标 → 点它 → 选 **"显示原文 / Show original"**;
- 如果是某个翻译插件干的,点那个插件 → 关闭翻译;
- 关完刷新一下页面(Windows 按 **F5**,苹果按 **Cmd+R**)。

### 第 3 步 · 新建仓库
登录后,点右上角 **＋ → New repository** →
- 起个名字(比如 `duanju-fenjing`);
- 选 **Private(私有)**;
- 点 **Create repository**。

---

## 【第二部分】把这套文件传进仓库

### 第 1 步 · 解压
把发给你的压缩包 `短剧自动化启动套件v3.zip` **解压**(解压不算装软件)。

### 第 2 步 · 上传(注意:空仓库这里有点特殊)
新建的空仓库页面上**没有**上传按钮,要用页面中间蓝色方框里的链接:
- 找到蓝框里那句 "…creating a new file or **uploading an existing file**";
- 点蓝色的 **"uploading an existing file"**;
- 进去后,**打开解压出来的 `duanju-kit` 文件夹,把里面的东西全选**(Windows: Ctrl+A / 苹果: Cmd+A),拖进网页中间那个虚线框;
- 往下拉到底,点绿色的 **Commit changes(提交)**。

> ⚠️ 小提示:`.claude` 这个文件夹如果拖不上去(有些电脑会隐藏以点开头的文件夹),**不影响使用**,直接跳过——整套流程都写在 `CLAUDE.md` 里了,照样能跑。

传完页面会变成文件列表,能看到 `CLAUDE.md`、`指令`、`剧本` 等——**就成功了**。

---

## 【第三部分】把剧本放进去

### 如果剧本是文字(推荐,最省事)
1. 点左边 **`剧本`** 文件夹打开;
2. 右上角 **Add file → Create new file**;
3. 最上面文件名那栏输入(前面的 `剧本/` 一定要带上):
   ```
   剧本/全本.txt
   ```
   (整本一个文件就叫 `全本.txt`;换别的剧就换个名,比如 `剧本/离婚那天.txt`)
4. 下面大框里,**把整本剧本从头到尾粘进去**;
5. 点绿色 **Commit changes** → 选默认的 **直接提交 main 分支** → 再点 **Commit changes**。

> 如果剧本是 Word/PDF 等文件:改用 **Add file → Upload files**,先在电脑上把文件重命名成干净的名字(去掉空格、括号、逗号),再拖进去提交。**但最省事还是转成 txt 粘贴。**

---

## 【第四部分】打开网页版 Claude Code,连上仓库

1. 浏览器打开 **`claude.ai/code`**,用**同一个账号**登录;
2. 输入框上方点 **Select repo(选择仓库)**;
3. 在列表里选你刚建的 `duanju-fenjing`。

> 如果列表里**没有**你的仓库:会看到一句 "Install the Claude GitHub app",点它 → 跳到 GitHub → 授权给你的账号(仓库范围选 All 或勾上这个仓库)→ 点绿色 Install/Authorize → 自动跳回来 → 再点 Select repo,就有了。

---

## 【第五部分】开跑

### A. 先拆解(出资产表)——每部剧跑一次
在对话框里粘这段发出去(文件名按你的剧本改):

```
读 CLAUDE.md,按「工作流 A · 拆解」,拆解 剧本/全本.txt,生成 8 个 sheet 的资产表 Excel(开篇说明 + 6 张表 + 出图风险提醒,角色卡含妆造版本行)。
跑完直接把生成好的 .xlsx 文件卡片给我,我点一下就能下载。不要新建分支、不要提交 GitHub。
```

### B. 出分镜——批量、每集给下载卡片
拆解看着没问题后,粘这段(文件名按你的剧本改):

```
读 CLAUDE.md,按「工作流 B · 分镜」,从 剧本/全本.txt 里,从第1集开始,一集一集连续往下出分镜表,每集生成一个 Excel(Sheet「分镜表」12 列 + Sheet「声线锁定」)。
要求:
1. 每出完一集,立刻在对话里把那一集的 .xlsx 文件卡片给我,我点一下能直接下载,然后接着跑下一集,不要停下来等我。
2. 不要新建分支、不要提交 GitHub。
3. 一集接一集往下跑,直到跑不动为止;如果遇到额度或其他限制停了,告诉我停在第几集。
```

### 跑的时候
- 中途弹出**确认 / 权限**按钮 → 点 **同意 / Allow**,它才继续;
- 每跑完一集会甩你一个 **xlsx 文件卡片**,**挨个点、下载到电脑存好**(别等,链接会过期);
- 它在云端跑,你可以合上电脑去忙别的;
- 万一它跑着跑着不给下载卡片了(改说"提交到仓库了"),回一句拽回来:
  > 继续,每集都用文件卡片给我下载,别提交 GitHub。

---

## 【怎么看还剩多少额度】

点右下角头像 → **Settings(设置)→ Usage(用量)**。记住三条:

1. **网页聊天和 Claude Code 共用一个账号的额度池**,不是分开的。
2. 有两道限制,谁先满卡谁:
   - **5 小时额度**:每 5 小时自动恢复。用完了歇会儿到点就满血,页面写 "Resets in X"。
   - **每周额度**:一周的大天花板,固定某天某点恢复(页面写 Resets 星期X)。这条满了,这一周基本跑不动,得等下周。
3. 打比方:5 小时额度=每顿饭量,吃撑歇会又能吃;每周额度=这个月伙食费,花光就得省。**跑之前先想好这周要出多少集,别一天用光。**

---

## 【换新剧本怎么办】

**仓库不用重建,永远用这一个。** 新剧本来了只做两件事:
1. 把新剧本传进 `剧本` 文件夹(起个新名字,如 `剧本/新剧.txt`);
2. 跑的时候,把上面命令里的 `剧本/全本.txt` **换成新文件名**,其它一个字不用改。

那套"引擎"(CLAUDE.md + 两份规范)是通用的,和具体哪部剧无关,放一次永久生效。

---

## 【常见卡点排查】(遇到就翻这里)

**Q:网页文字/文件夹名看着乱七八糟?**
A:浏览器自动翻译在捣乱。地址栏翻译图标 → 显示原文 / 关插件 → 刷新。

**Q:空仓库页面找不到 "Add file" 按钮?**
A:空仓库才这样。用页面中间蓝框里的 **"uploading an existing file"** 链接上传;传完有文件了,以后右上角就有 Add file 了。

**Q:文件拖进去了,页面没反应、没生效?**
A:多半是忘了拉到最底下点绿色 **Commit changes**。补点一下;或按 F5(苹果 Cmd+R)刷新看有没有传上。

**Q:它跑完说"提交到某个分支了 / 让我 Create PR",我不想搞这些?**
A:说明你的命令没加"不要新建分支、给下载卡片"。用上面第五部分那两段命令(带这句的),它就直接给下载卡片、不碰 GitHub 了。

**Q:苹果电脑有什么不一样吗?**
A:基本没有。网页操作完全一样,只是**快捷键 Ctrl 换成 Cmd**(全选 Cmd+A,刷新 Cmd+R)。装不装英特尔/M芯片都不影响,因为不在本地装东西。

**Q:一次跑太多集,后面几集质量变差?**
A:一个会话连续跑几十集,AI 的"记忆桌面"会越堆越满,后面可能忘设定。稳妥做法:**一个会话跑一二十集,然后开 New session,让它从下一集接着跑**(命令里把"第1集"改成"第21集")。

---

## 一句话总纲

> **建一个仓库(永久用)→ 传进这套文件 + 你的剧本 → 打开 claude.ai/code 连仓库 → 拆解命令出资产表、分镜命令批量出分镜 → 每集点卡片下载 → 导进飞书。**
> 全程浏览器,不装东西。换剧本只改命令里的文件名。
