# 短剧拆解 + 分镜 自动化项目

这个仓库用来把「短剧剧本」自动加工成两类产物:

1. **资产拆解表**(1 个 xlsx,8 个 sheet:开篇说明 + 6 张表 + 出图风险提醒)
2. **逐集分镜表**(每集 1 个 xlsx,核心是 12 列分镜表)

你(Claude)在本仓库里工作时,必须严格遵守 `指令/` 文件夹里的两份规范,并按本文件描述的工作流产出文件。

## 目录结构

- `指令/剧本拆解模板.md` — 拆解阶段完整规范(v3.0 妆造版本·四视图版;5 条拆解原则;表0 + 6 张表;妆造版本行;公职合规)。**本仓库只执行其中「第二部分·拆解阶段核心指令」并以「第四部分·字段详解」校准粒度;第三部分是给出图 AI 用的,不在本仓库执行。**
- `指令/分镜师手册.md` — 分镜阶段完整规范(AI 漫剧分镜提示词生成总 Prompt;12 列分镜表;声音字段互斥铁律;角色声线锁定;位置承接;逐镜自检)
- `剧本/` — 放剧本原文。整本存 `全本.txt`,或每集分别存 `第01集.txt`……;不同剧本用不同文件名。
- `资产表/` — 拆解产物输出到这里
- `分镜表/` — 分镜产物输出到这里

## 通用规则(两条工作流都适用)

- 所有产物用 **.xlsx** 输出(用 Python 的 openpyxl 生成),不要只给 markdown 表。规范里「无法生成 Excel 时可改输出 Markdown 表」的降级条款在本仓库**不适用**——这里永远直接生成 xlsx。
- sheet 名、列名、列的顺序、表的数量,**一律以 `指令/` 里对应规范和本文件为准,不增不减不改顺序**。
- 集号一律两位补零:第01集、第02集……
- 每个 sheet 冻结表头行;列宽适当加宽,长文本单元格自动换行。
- **自检纪律(最重要)**:规范里凡是写了「必须 / 禁止 / 不允许 / 自检」的条款,你必须**真的执行**——先生成,再逐条对照自检,发现不合格就自动修正 / 重拆那一部分,直到达标。自检过程不必输出,但**绝不把不合格的结果当成品交付**。

## 工作流 A · 拆解(命令:`/chaijie`)

**输入**:`剧本/` 下的剧本(默认整本;若指定了文件名就用那个)。

**步骤**:
1. 先**完整通读整部剧本**,不要边读边写;先为每个 A 类角色数清全剧的妆造版本数,再开始填表。
2. 严格按 `指令/剧本拆解模板.md` 第二部分绿框指令拆解,产出:表0 开篇说明 + 6 张表 + ⚠️出图风险提醒(全剧最易穿帮的 3-5 个风险点)。字段粒度对照第四部分详解、第五部分示例。
3. 五条拆解原则全守,重点盯三条:
   - **原则4 出图友好**:手持/怀抱/背挎类道具、具体动作姿势、戏剧化表情,一律不写进角色卡,统一落到表3「关键动作」栏。
   - **原则5 妆造版本(v3.0 核心)**:身份/阶级变化、时间/年龄跨度、整场核心造型才立版本行(堆加在该角色主行正下方,编号如 A1-老板版);受伤/被打/淋湿/弄脏等**瞬时状态一律不立版**,写进「关键动作」栏;版本按剧情阶段归并,单角色全剧通常 2~5 版,严禁一集一版。版本行的面部特征固定写【与主行同一张脸,只换服装/气色】。
   - **公职合规**:公职角色服装栏一律写「无标识素色行政夹克(职级较高)/ 无标识中山装(职级较低)」,从源头不出现国徽/警徽/肩章等任何国家标识。
4. 表3「角色标注」必须带版本名(如 A1-老板版首造 / 复用-A1-老板版);表5 按**版本一行**独立追踪首次出现与复用,总行数 = 全剧人物出图张数(即出图排产清单)。
5. 剧本太长一次放不下时,分批拆,最后合并并统一编号。

**输出**:`资产表/<剧名>_资产拆解表.xlsx`,8 个 sheet,顺序固定:
开篇说明 / 表1 核心角色卡(13列·含妆造版本行) / 表2 核心场景卡(9列) / 表3 分集索引表(7列) / 表4 资产溯源-场景(8列) / 表5 资产溯源-角色(8列·按版本一行) / 表6 一次性人物清单(6列) / 出图风险提醒。

## 工作流 B · 分镜(命令:`/fenjing <集号>`)

**输入**:`剧本/第<集号>集.txt`(或整本里对应的一集)＋ `资产表/` 里的角色卡(含妆造版本)和场景卡——用于统一 @素材绑定 命名与造型版本;若无对应资产表,则从剧本自行推导人物/场景/造型清单。

**步骤**:把这一集剧本当作手册所说的【输入文案】,严格按 `指令/分镜师手册.md` 全部规则执行。重点不能漏:
- **按节组织**:每节约 12-15 秒,原则上单节不超过 8 镜;每节所有镜头【时间】之和必须等于该节总时长;节尾要有明确落点(问题/反转/动作启动/新目标/危机升级/钩子);信息量过大自然拆到下一节,不许硬塞。
- **12 列固定表头、顺序禁改**:节 | 镜头 | 场景/时间/光线 | 时间标识 | 运镜 | 画面 | 动作/情绪 | 位置承接 | 音效 | 台词 | 时间 | @素材绑定。【时间】【@素材绑定】固定为最后两列,填法按手册「二点五」硬规则。
- **声音字段互斥铁律**:不设 OS 列 / 画外音列——OS、画外音统一并入台词列,用【OS】【画外音】前缀;一个镜头的台词单元格只允许一种声音;原文「内心 OS + 开口回应」必须拆成 OS 镜 → 台词镜,禁止同格混填。
- **台词按硬格式写、重要信息一句不丢**:人物(固定声线锚点;情绪+语气+语速/停顿):"台词内容";台词>35 字必拆、OS>28 字必拆,按信息点断句;2 秒内不塞长句;一镜尽量只一人说话,对话拆「说话镜 + 反应镜」。
- **角色声线锁定**:本集出现新发声角色(含非人角色/小动物)时,先建立固定声线锚点;老角色只沿用不重设;锚点首次完整、后续压缩。
- **场景与画面白描**:场景词只写物理空间,同一场戏称呼稳定;画面只写能拍到的具体内容,避免抽象描述,易违规描述按手册「十四」替换;入镜人物必须写清位置承接,**未入镜人物禁止出现在画面/位置承接/素材绑定里**。
- **@素材绑定命名全片统一**:格式 `@人物1 @人物2 / @场景`;空镜写 `/ @场景`;画外音角色写 `@某某的声音`;人物造型用中文括号标注(如 @周砚(喜服));**有资产表时,人物/场景/造型命名与资产表的角色·妆造版本·场景命名对齐**,全片不允许两种写法。
- **产出后逐镜自检**:执行手册「十七点五·输出前声音自检」全部条款 + 各章「必须/禁止」硬规则;发现声音冲突优先拆镜,禁止靠删信息解决;不达标修正后再交付。

**输出**:`分镜表/第<集号>集_分镜表.xlsx`:
- Sheet「分镜表」:12 列,严格按手册列规范;冻结表头;按**节**交替行底色,便于逐节阅读。
- Sheet「声线锁定」:两列(人物 | 固定声线锚点),汇总本集全部发声角色的锚点(新建的 + 沿用的都列全),供下一集续跑时保持声线一致。

## 连续跑多集时

- 从第 2 集起,先读上一集分镜表(至少末节 + 声线锁定 sheet),承接人物站位、道具归属、情绪状态、空间关系与既有声线锚点,再开始本集;@素材绑定 命名与前几集完全一致。
- 每集单独一个 xlsx,产出一集交付一集,不要合并、不要攒着。

## 触发方式

- 输入 `/chaijie` 跑拆解;`/fenjing 3` 出第3集分镜。
- 也可以**直接用中文说**,例如:「按 CLAUDE.md 的拆解流程,拆解 剧本/全本.txt」,或「从 剧本/xxx.txt 第1集开始连续往下出分镜,每集给我下载卡片,别提交 GitHub」。
